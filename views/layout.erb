<html>

<head>
	<!--<link href="style.css" rel="stylesheet" type="text/css"> -->
	 <title><%= @title || "welcome"%></title>
</head>
<body>

<div >

<div style="float:left; margin-left: 75px; height: 430px;" >
	<img src="/images/index.png" alt="picture" width = 700 height = 430 >
</div>

<div style="float:right; margin-right: 75px; height: 430px; ">
<div><font >Student</font></div>
<div style="margin-top: 40px"><font >Information</font></div>
<div style="margin-top: 40px"><font >System</font></div>
</div>

</div>

<div style="margin-top: 500px; margin-left: 100px">
<ul id="menu">
         <% if @admin %>
         <li><a href="/students/main">Students</a></li>
         <li><a href="/comments/main">Comments</a></li>
         <li><a href="/video">Video</a></li>
         <li><a href="/logout" class="last">Logout</a></li>
         <% end  %>
</ul>
</div>
<div style="margin-left: 100px; margin-top: 600px">
  <%= yield %>  
</div>


</body>

<style type="text/css">
	html {background-color: #AAAAAA;} 
body {
background-color:#DDDDDD; margin:2em auto 2em; padding:25px; position:relative;
} 
font {
color:#A42D00; position: relative; font-weight:900; font-size:550%; }


#menu { 
            font:12px verdana, arial, sans-serif; /* 设置文字大小和字体样式 */
            width: 100%;
        }
        #menu, #menu li {
            list-style:none; /* 将默认的列表符号去掉 */
            padding:0; /* 将默认的内边距去掉 */
            margin:0; /* 将默认的外边距去掉 */
            float: left; /* 往左浮动 */
            display: block;
        }
        #menu li a {
            display:inline-block; /* 将链接设为块级元素 */
            width:150px; /* 设置宽度 */
            height:60px; /* 设置高度 */
            line-height:60px; /* 设置行高，将行高和高度设置同一个值，可以让单行文本垂直居中 */
            text-align:center; /* 居中对齐文字 */
            background:#3A4953; /* 设置背景色 */
            color:#fff; /* 设置文字颜色 */
            text-decoration:none; /* 去掉下划线 */
            border-right:1px solid #000; /* 在左侧加上分隔线 */
        }
        #menu li a:hover {
            background:#146C9C; /* 变换背景色 */
            color:#fff; /* 变换文字颜色 */
        }
        #menu li a.last {
            border-right:0; /* 去掉左侧边框 */
        }


ul li {
    list-style: none;
}
a {
    text-decoration: none;
}

</style>
</html>

